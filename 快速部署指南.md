# 🚀 快速部署指南

## 📝 部署前检查清单

✅ 确保以下文件都在项目中：
- `app.py` - 主应用文件
- `wiper_data.xlsx` - 数据文件
- `requirements.txt` - 依赖列表
- `README.md` - 项目说明

## 🌐 GitHub + Streamlit Cloud 部署步骤

### 1️⃣ 推送到 GitHub

```bash
# 初始化 Git（如果是新项目）
git init

# 添加所有文件
git add .

# 提交
git commit -m "雨刷尺寸查询系统"

# 创建主分支
git branch -M main

# 添加远程仓库（替换成你的 GitHub 仓库地址）
git remote add origin https://github.com/你的用户名/仓库名.git

# 推送到 GitHub
git push -u origin main
```

### 2️⃣ 在 Streamlit Cloud 部署

1. 访问：https://share.streamlit.io
2. 用 GitHub 账号登录
3. 点击 "New app"
4. 选择你的仓库
5. 设置：
   - Repository: `你的用户名/仓库名`
   - Branch: `main`
   - Main file: `app.py`
6. 点击 "Deploy!"

### 3️⃣ 完成！

等待 2-5 分钟，你的应用就上线了！

你会得到一个类似这样的 URL：
```
https://你的用户名-仓库名-xxxxx.streamlit.app
```

## 🔧 本地测试

在部署前，先本地测试：

**Windows:**
```bash
# 安装依赖
python -m pip install -r requirements.txt

# 运行应用
python -m streamlit run app.py

# 或直接双击运行 start.bat
```

**Mac/Linux:**
```bash
# 安装依赖
pip install -r requirements.txt

# 运行应用
streamlit run app.py

# 或运行脚本
chmod +x start.sh
./start.sh
```

## 🔄 更新应用

修改代码后：

```bash
git add .
git commit -m "更新说明"
git push
```

Streamlit Cloud 会自动更新你的应用！

## ⚠️ 常见问题

**Q: 数据文件没有上传？**
```bash
# 确保数据文件已添加
git add wiper_data.xlsx
git commit -m "添加数据文件"
git push
```

**Q: 部署失败？**
- 检查 requirements.txt 是否正确
- 确保仓库是公开的
- 查看 Streamlit Cloud 的部署日志

**Q: 中文显示乱码？**
- 确保所有文件都是 UTF-8 编码保存

## 📞 需要帮助？

查看详细文档：
- `DEPLOY.md` - 完整部署指南
- `README.md` - 项目说明

---

祝你部署成功！🎉

